pwa:
  caches:
    font:
      strategy: network-first
    image:
      strategy: network-first
    script:
      strategy: network-first
    style:
      strategy: network-first

hugopress:
  modules:
    pwa:
      disable: true

decap_cms:
  local_backend: true
  backend:
    name: git-gateway
  site_url: http://localhost:1313
  display_url: http://localhost:1313

